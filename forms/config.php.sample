<?php
/**
 * Copy this file to config.php and fill in the real values.
 * config.php is gitignored - credentials must never be committed.
 */

// --- SMTP transport ---------------------------------------------------------
define('SMTP_HOST', 'smtp.gmail.com');
define('SMTP_PORT', 587);
define('SMTP_USER', 'you@example.com');

// Gmail App Password: https://myaccount.google.com/apppasswords
define('SMTP_PASS', 'REPLACE_WITH_NEW_APP_PASSWORD');

// --- Addressing -------------------------------------------------------------
define('MAIL_FROM', 'you@example.com');
define('MAIL_FROM_NAME', 'Sadhav Offshore');
define('MAIL_TO', 'recipient@example.com');

// --- Attachments ------------------------------------------------------------
// Limits match what each form tells the visitor on the page.
define('RESUME_MAX_BYTES', 5 * 1024 * 1024);   // careers.html says 5MB
define('RESUME_ALLOWED_EXT', 'pdf,doc,docx');

define('RFQ_MAX_BYTES', 10 * 1024 * 1024);     // rfq.html says 10MB
define('RFQ_ALLOWED_EXT', 'pdf,docx,zip');
